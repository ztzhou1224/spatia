# Spatia environment variables
#
# Copy this file to `.env` and fill in values as needed.
# Variables left blank are optional unless noted otherwise.

# --- AI (Gemini) ---
# Required only for features that call Gemini via `spatia_ai::GeminiClient::from_env`.
SPATIA_GEMINI_API_KEY=

# --- Geocoding (Geocodio) ---
# Required when geocode cache misses occur (engine fallback to Geocodio API).
SPATIA_GEOCODIO_API_KEY=

# Optional Geocodio API base URL override (useful for testing/mocking).
# Default: https://api.geocodio.com
SPATIA_GEOCODIO_BASE_URL=https://api.geocodio.com

# Optional max addresses per Geocodio batch request.
# Default: 100
# Valid range after clamping: 1..10000
SPATIA_GEOCODIO_BATCH_SIZE=100

# --- Overture ---
# Optional Overture release to read from S3.
# If unset, engine uses its built-in pinned release constant.
# Example: 2025-10-22.0
SPATIA_OVERTURE_RELEASE=

# --- Tauri/Vite dev ---
# Used by Vite HMR host config during `tauri dev`.
# Usually set automatically by Tauri; leave empty unless you need a custom host.
TAURI_DEV_HOST=
