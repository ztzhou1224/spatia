[package]
name = "spatia_overture"
version = "0.1.0"
edition = "2021"

[dependencies]
duckdb = { version = "1.4.4", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
spatia_geocode = { path = "../geocode" }
